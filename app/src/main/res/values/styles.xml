<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>

    <style name="Twitter.Theme" parent="AppTheme">
        <item name="android:windowBackground">@color/gray_1000</item>
    </style>

</resources>
